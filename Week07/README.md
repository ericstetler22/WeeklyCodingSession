# Week 7 — Data Shape Matters

**Principle:** Never trust external data.

## Goal
Harden a script that consumes device JSON with missing fields.

## Run
`powershell.exe -ExecutionPolicy Bypass -File .\starter.ps1 -InputFile .\data\\devices_bad.json`
