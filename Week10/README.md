# Week 10 — Config Over Code

**Principle:** Don’t hardcode behavior.

## Goal
Load settings from JSON config and validate it.

## Run
`powershell.exe -ExecutionPolicy Bypass -File .\starter.ps1 -ConfigPath .\config\\settings.json`
