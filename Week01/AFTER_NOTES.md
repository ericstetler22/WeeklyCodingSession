# Expected "After" Example

This file represents a reasonable "end of session" outcome.

## Key improvements
- Introduces parameters (input/output/prefix) instead of magic values.
- Adds defensive checks for file existence and empty JSON.
- Uses intent-revealing function names and a clear flow.
