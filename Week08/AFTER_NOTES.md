# Expected "After" Example

This file represents a reasonable "end of session" outcome.

## Key improvements
- Imports a shared module rather than duplicating Graph behavior.
- Encapsulates device retrieval behind one function.
- Uses a retry wrapper and consistent logging for reliability.
