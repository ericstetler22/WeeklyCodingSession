# Expected "After" Example

This file represents a reasonable "end of session" outcome.

## Key improvements
- Rejects wildcards and enforces a maximum target count (fleet safety).
- Uses SupportsShouldProcess so the team can practice -WhatIf.
- Adds structured logging with run correlation id.
