# Week 12 — Capstone: Intune-Style Remediation

**Principle:** Combine everything.

## Goal
Build a remediation-style script with config, query, validation, idempotent action, logging, dry-run, and error handling.

## Run
`powershell.exe -ExecutionPolicy Bypass -File .\starter.ps1 -ConfigPath .\config\\settings.json -DryRun`
